import { user } from '../types';
//import http from '../../utils/http';

export default {
    /***
     * change credit
     */
    [user.CHANGE_CREDIT] ({ commit }, data) {
//        http.post('/api/user/credit', data)
//            .then((result) => commit(user.CHANGE_CREDIT_SUCCESS, result))
//            .catch((error) => commit(user.CHANGE_CREDIT_FAILURE, error));
    }
};
